version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "REACT_APP_BASE_URL=http://kviki-env.eba-b6newnia.us-west-1.elasticbeanstalk.com/api" >> .env

    build:
      commands:
        - npm install
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - "**/*"
      - public/sitemap.xml
      - public/robots.txt
  cache:
    paths:
      - node_modules/**/*
